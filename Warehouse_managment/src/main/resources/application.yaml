server:
  port: 8081

spring:
  application:
    name: Warehouse_managment
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/warehouse_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Ho_Chi_Minh
    username: root
    password: "Qu@ngminh80"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
jwt:
  secret: MySuperSecretJWTKey
  expirationMs: 86400000
